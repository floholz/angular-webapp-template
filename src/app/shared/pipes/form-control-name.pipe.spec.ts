import { FormControlNamePipe } from './form-control-name.pipe';

describe('FormControlNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FormControlNamePipe();
    expect(pipe).toBeTruthy();
  });
});
